import React from 'react';
import { Navbar } from 'react-bootstrap';

const Footer = () => {
    return (
        <Navbar fixed='bottom'>
            something
        </Navbar>
    )
}

export default Footer;